# Marker Templates

Store LightGlue template PNGs here, organized by camera.

Expected layout:

- `templates/markers/cam1/id_<MARKER_ID>.png`
- `templates/markers/cam2/id_<MARKER_ID>.png`

Each camera folder should contain the marker template images used by that camera's `lightglue.template_dir` setting.
